<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <appender name="STDOUT_COLOR" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%magenta(%d{yyyy-MM-dd HH:mm:ss.SSS}) %gray([%thread]) %highlight(%-5level) %cyan(%logger -) %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="STDOUT_NO_COLOR" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>
   
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.classiga/logs/classiga-commons/output.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.classiga/logs/classiga-commons/output.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>    
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender> 
   
    <root level="DEBUG">
        <if condition='property("os.name").toLowerCase().contains("windows")'>
            <then>
                <appender-ref ref="STDOUT_NO_COLOR"/>
            </then>
            <else>
                <appender-ref ref="STDOUT_COLOR"/>
            </else>
        </if>
        <appender-ref ref="FILE" />
    </root>
    
</configuration>